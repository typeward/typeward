# UI Overhaul — Status

| Phase | State | Date | Summary |
|---|---|---|---|
| **A — Foundation** | **complete** | 2026-05-15 | Density tokens (`density.css`), motion kill-switch (`motion.css`), `ui-store`, `workspace-store`, `nav-store`, Rust settings extended with `ui` + `workspace` sections (serde-defaulted for upgrade safety). `/design` folder seeded. No visible UI change yet. |
| **B — Chrome cleanup** | **complete** | 2026-05-15 | Traffic lights removed from `TopBar` + `EditorTopBar`. Brand + workspace pill + `<ChevronsUpDown>` import removed from `TopBar`. Chrome icons now size-driven by `--ui-icon-chrome` via `.ui-icon-chrome` utility. Settings ↔ Editor route memory via `nav-store`: back button reads `previousRoute()`, falls back to `/projects`. |
| **C — Projects** | **complete** | 2026-05-15 | Cards redesigned (no thumbnails, gradient-glow hover, density-driven sizing). Sidebar: Storage removed, Spaces/Tags gated by settings, Upload→Import rename, single bottom Free-plan pill. Toolbar: Widgets dropdown (replaces Composer hero + Resume Editing + Import button + filter chips), Sort menu (5 keys), Cards/List view toggle. Right rail rewritten as slide-in NotificationsPanel toggled by bell. Widget shelf with 9 registered widgets (Compose + Recent + Pinned Notes functional; 6 stubs). KbdHint primitive with platform-aware tokens, hidden on tablet. |
| **D — Editor** | **complete (core)** | 2026-05-15 | Project switcher → recent dropdown. Local avatar removed (collaborators only). Layout menu wired with split/editor/preview + drawer/pdf-tab. PDF toolbar: page-nav and zoom moved right, AI toggle + Console toggle (when in pdf-tab mode), Export menu (UI stubs) replaces Download. Preview pane switches between PDF, Console (LogsDrawer), and AI views via `previewMode` signal. AiView stub created. Status bar shows `Ln X, Col Y` driven by CodeMirror updateListener → editor-view-store. Save + Compile buttons removed from tab strip. Sidebar: file action icons moved to LEFT of tab row, "Format" → "Engine" (LaTeX-only), max-width 320px. Bibliography + Chat tabs removed from LogsDrawer. **Deferred**: zoom redesign (custom input + slider + fit-width/height), floating format panel, custom scrollbar styling, SyncTeX double-click, working Export pipelines. |
| **E — Settings + themes** | **complete (core)** | 2026-05-15 | 7 new themes ported (Catppuccin, Dracula, Gruvbox, Mono, Nord, Solarized Light, Tokyo Night). Paper updated to use Light prototype tokens. Theme picker shows all 11 in a 4-col grid. Density picker wired to ui-store (Compact/Cozy/Comfortable). Animations toggle wired. Workspace card surfaces Enable Spaces, Enable Tags, Notifications-panel-default. Custom themes card with enable toggle grays out built-in pickers; JSON discovery + folder watcher deferred. Default-engine row hidden on tablet/mobile (texlive-wasm forced); on desktop, texlive-wasm removed from the selector (it's the mobile fallback). **Deferred**: custom theme JSON loader (folder watcher + parse + activation), AI provider config row. |
| **F — Typography sweep** | **complete** | 2026-05-15 | Density scale re-anchored on whole-pixel 12/14/16 body (Compact/Cozy/Comfortable). Full 5-step scale (`xs/sm/base/lg/xl`) + line-height + letter-spacing tokens following Material 3 / HIG / SaaS conventions. `--ui-icon-menu` + `--ui-icon-sm` tokens added with `.ui-icon-menu` / `.ui-icon-sm` utility classes; body defaults set via `tokens.css`. Bulk-rounded every fractional `text-[Xpx]` (88 occurrences across 22 files) to nearest whole pixel. Projects sidebar items, Settings sidebar nav, and Editor sidebar tabs/search migrated to `--ui-font-base` + density-aware icon classes — they now render 12 / 14 / 16 px depending on density (was 12.5px static). |
| **G — Desk Lamp recolor** | **complete** | 2026-06-11 | Color redesign from the `design_files/t1` + `t2` "Desk Lamp" prototypes. New **Daylight** theme (t1 light) is the app default; new **Lamplight** theme (t1 dark: warm black `#0D0C0A`, parchment text, amber `#E8A34D` with lamp-glow ambient blobs). Obsidian + Graphite retired. New tokens: `--syntax-*` (per-theme CodeMirror palette — every theme now defines one), `--color-accent-fg` (text on accent surfaces; `.accent-grad` applies it, so light accents like Lamplight's amber get ink labels), `--color-glass-soft-fill`. Hardcoded violet/cyan glows and white-alpha surfaces swept app-wide to `color-mix(...accent...)` / control tokens (~20 files, incl. full Onboarding light-theme pass). `glow-violet` → `glow-accent`. Accent picker first entry relabeled "Theme default" (no `data-accent` = theme's native accent). |
| **H — Identity tune + theme trim** | **complete** | 2026-06-11 | Theme set reduced to exactly **Daylight / Lamplight / Aurora / Paper** — the 7 community ports (Catppuccin, Dracula, Gruvbox, Mono, Nord, Solarized Light, Tokyo Night) removed per user direction; persisted picks fall back to Daylight. Daylight retuned to `design_files/sample_identity.txt` + `sample.png`: ivory `#F8F4EA` bg, charcoal `#22211E` ink, solid `#D8D0C2` paper-gray hairlines, **near-black `#101210` primary actions** (near-flat `.accent-grad`, ivory `--color-accent-fg`), aged-brass `#B79A5C` selection/warn warmth, seal-red `#A84935` errors, paper-toned format tints (brass LaTeX / sage Typst) — saturated tech colors out of the chrome, blue confined to code syntax. New `--color-text-selection` token (global `::selection` + CM); `LIGHT_THEMES` export replaces the stale hardcoded light-theme list in `text-shell.tsx` `mdTheme`. Window `backgroundColor` → `#F8F4EA`. |
| **I — Review remediation** | **complete** | 2026-06-11 | "Fix everything" pass over the full-app review. Correctness: Mod+Enter no longer double-fires CM's `insertBlankLine` alongside compile (stray blank line saved to disk); settings.json values validated against current enums (removed themes fall back to Daylight); review-comment store got a real project lifecycle (desktop load, reset on switch, flush-to-correct-project on debounce); dirty-tab close confirms; PDF zoom race fixed; project-open error path tears down the previous project's runtime. Visual: boot splash re-tints from persisted theme (`src/boot-theme.ts`); checked Switch thumbs use `--color-accent-fg`; `.md-preview` prose styles exist now; status/severity colors swept to ok/warn/err tokens (LogsDrawer, CommitPanel incl. invalid `rgb()22` hex-alpha, SyncStatusBadge, EditorTopBar, Integrations pills, Settings danger zone); `.lift.accent-grad:hover` accent-fg wash; `--color-overlay-scrim` + `--color-text-selection`/`--accent-text-*` tokens. UX: placebo Settings rows disabled + "soon"-pilled (incl. Notifications preview), live Keyboard-shortcuts panel from the registry, Issues cards click-to-jump + console auto-surfaces on compile failure, onboarding honesty rewrite (probe-before-skip, real summary instead of fake installer, dead buttons removed), Projects de-mock (dead nav removed, sample Spaces/Tags opt-in, working sorts only, real bell count/tier pill), New-file creation in the editor sidebar, GitLab no longer misrouted to GitHub auth, destructive dialogs confirm. A11y/tablet: global `:focus-visible`, palette dialog/listbox semantics + Tab trap, `inert` notifications panel, tab roles + whole-tab hit areas, shared Escape+outside-click dismiss for all hand-rolled popovers (`src/lib/dismiss.ts`), touch sync-mode toggle for inverse SyncTeX, 44px format-toolbar targets on tablet, pane swipe clamps at ends. Infra: CI checks out + builds the texlive-wasm sibling, new tests.yml (vitest + cargo test on push/PR), `.gitattributes`, Vite 6 `defaultClientConditions` fix, dead `@types/*` dropped, telemetry `kind` capped, tectonic capability validator tightened, CSP gained `*.supabase.co` and dropped vestigial `asset:`. Deferred: Projects/Settings tablet-responsive layouts (real design effort, not a fix). |

| **J — Composition pass** | **complete** | 2026-06-11 | The three targeted redesigns from the full-app review, per user direction. **Projects**: ComposerHero deleted outright — the library is the hero (serif "Library" heading + count); widget shelf stays opt-in with a curated, fully-working roster (Recent projects, Library summary, Pinned notes, real Pomodoro Focus timer) — the six stub widgets removed. **Focus mode**: `core.toggleFocusMode` (Mod+Shift+F) strips top bar, sidebar, tab strip, format toolbar, status bar, and logs drawer down to source + page, with a hover Exit pill; session-scoped. **Display typography**: Source Serif 4 (`--font-display` / `font-display`) on the Library heading, onboarding headline + pane h2s, Dialog titles, NoProject card — rollback recipe in design/themes.md. **Settings**: nav restructured — placeholder sections (Profile, Team, Language, Billing, Usage) removed; new "Integrations" group with References / Cloud storage / Git & GitHub / AI providers / Grammar subcategories (IntegrationsPanel takes a `section` prop); tier pill moved to "Account & plan". **Editor**: SCM tab renders only when the project root has `.git` (bounces to Files on switch). **Supabase staging fixed**: the project was completely empty — both migrations applied (named to match local files), seed + entitlements matrix pushed, test@test.cz existed but had nothing behind it; subscription seeded to Pro, profile backfilled, advisor-flagged `handle_new_user` RPC grant revoked (new migration `20260611000001_harden_function_grants`, mirrored locally). Login now works end-to-end. |

| **K — Integration friction pass** | **complete** | 2026-06-11 | Serif display typography reverted per user direction (re-apply recipe kept in design/themes.md). **Login root cause found**: GoTrue auth succeeded but persisting the session died — Windows Credential Manager caps a blob at 2560 bytes (UTF-16, ~1280 chars) and the Supabase session bundle is 2–4 KB; the serialized `CredentialError` rejection rendered as "[object Object]". New chunked keyring layer (`integrations/auth/chunked.ts`, ≤1024-char parts + marker slot, 6 tests) backs both the session storage adapter and the entitlements cache; sign-in errors render readably via `describeIpcError`. **Zotero**: Better BibTeX is now optional — the local provider falls back to Zotero 7's built-in API (`127.0.0.1:23119/api/users/0/items?format=bibtex`, paginated via Total-Results); renamed "Zotero (local)", settings row reflects which path answered. **Ollama**: zero-config — the AI row probes the daemon (default `localhost:11434`), shows detected model names with a Refresh, and only offers the custom-URL field when detection fails (it previously hardcoded "ready" and always demanded a base URL). |

| **L — Interaction polish** | **complete** | 2026-06-12 | Inverse SyncTeX is now **double-click** on the PDF page (double-tap on touch; Shift+click kept as the power shortcut) — the toolbar crosshair toggle is gone. Editor text-selection contrast bumped (Daylight brass 0.30→0.45, Lamplight amber 0.40, default accent mix 32%). **Refs sidebar tab hides** until a citation provider is configured (mirrors the SCM-tab git gating). **Nav bug fixed**: Editor → Settings → back stranded users on "No project open" because `previousRoute` stored a bare `/editor` without `?path=` — the full URL is stored now. **Auto-compile on save** is real (rides `saveActiveFile`; compile's own save path can't recurse) and the Settings row is live. **Export PDF** is real (save dialog + byte copy; dialog-picked paths extend the fs scope); the other export options stay disabled+soon. **Infrastructure moved out**: `app/infrastructure/` deleted (gitignored against return), contents committed as the root commit of the dedicated `typeward/infrastructure` repo at `../infrastructure` with an updated README; CLAUDE.md references updated and Phase 7.5 marked done. |

| **M — Soon-list burn-down** | **complete** | 2026-06-12 | Five formerly-"soon" items are real. **Vim mode** via `@replit/codemirror-vim` (compartment-reconfigured, precedes other keymaps; Settings toggle live). **Stop on first error**: new `stopOnFirstError` editor setting (serde-defaulted true to match the previously hardcoded behavior) plumbed through `compile_latex` — toggling OFF drops `-halt-on-error` from latexmk/pdflatex so one pass collects every diagnostic; Tectonic always halts. **Pinned notes** persist in localStorage. **Source bundle (.zip)** export: new `export_project_zip` IPC reuses the template-capture walk (skips `.git`/`.typeward`/symlinks/build junk), writes into the project's own `.typeward/build/` sidecar (no new arbitrary-write primitive), and the frontend copies it to a dialog-picked destination — same pattern as Export PDF. **Reset local app data** works: confirm dialog → `reset_settings` IPC (defaults via `Settings::default()`) + localStorage.clear() + reload. Remaining soon-list (each a real feature): notifications system, custom-theme JSON loader, pandoc docx/html export + annotation flattening, Stripe webhook (7.6), tablet layouts for Projects/Settings. |

| **N — Plans, AI switch, custom themes, dashboard** | **complete** | 2026-06-12 | Four user-directed changes. **Locked features hide**: `<FeatureGate>` renders nothing by default for missing entitlements (the `UpgradePrompt` component and the Locked rows in Integrations settings are deleted) — users on a lower plan no longer see chrome for features their tier doesn't include; free tier thus shows Zotero local + JabRef + DOI, iCloud only, Ollama only. AiView's empty-state copy dropped its paid-plan marketing. **AI master switch**: new `integrations.ai.enabled` (default on, serde-defaulted) — when off, the AI toolbar toggle and chat pane disappear from the editor (`previewMode` bounces ai→pdf), the provider registry deactivates (zero AI traffic), and the AI settings card collapses to the switch. **Custom themes shipped** (was the deferred JSON loader): Rust `themes.rs` scans/validates `<app_data>/themes/*.json` (name + base + ≤200 tokens; key/value guards against style injection; per-file warnings surface in Settings), `custom-themes.ts` applies the active theme by setting the built-in base + injecting an `html[data-custom-theme]` style block; Settings card has Open folder / Create sample / Reload and a live theme-tile grid; sample theme **Harbor** (deep-sea teal on Lamplight) doubles as the authoring reference; 5 Rust tests. **Dashboard panel replaces the widget shelf**: one opt-in panel (toolbar "Dashboard" toggle, persisted) holding a fixed Activity card (project count, format chips, "Continue <last project>" — absorbs the old Library-summary widget) plus the Recent/Notes/Timer cards — individually toggleable via a Customize menu and **drag-reorderable** (HTML5 DnD, order persisted as `workspace.dashboardOrder`); `WidgetsShelf`/`WidgetsMenu` deleted. |

| **O — JabRef + in-app billing removed** | **complete** | 2026-06-13 | Two scope cuts. **JabRef gone**: the file-based `.bib` CitationProvider, its `references.jabref` settings field (frontend ipc type + Rust `JabRefSettings` struct), the IntegrationsPanel row, the `integrations.references.jabref` free-tier entitlement key, the init wiring, and every UI/comment mention deleted — Zotero's local library covers the same workflow. (Supersedes row N's "free tier shows … JabRef".) **In-app billing dropped**: the app sells nothing — no Stripe code, checkout, or webhook (the planned 7.6 webhook is retired from the app; it belongs to the website). Plans are purchased/managed on the Typeward website; the signed-in Account section now has a "Manage plan & billing" button → `https://typeward.app/account` (allowlisted in `opener:allow-open-url`), and the sign-in card links out to create an account. `database.types.ts` drops the `stripe_price_id` / `stripe_subscription_id` / `stripe_customer_id` columns (the live tables keep them for the website's webhook; the app never selects them). |

| **P — iCloud Drive integration removed** | **complete** | 2026-06-20 | iCloud Drive dropped as a cloud option. It was display-only — OS-mediated, no `CloudFsProvider`, never part of the `CloudProviderId` union. Deleted `src/integrations/cloud/icloud/`, the `ICloudRow` in `IntegrationsPanel` + its card-subtitle mention, and the `integrations.cloud.icloud` free-tier entitlement key; CLAUDE/AGENTS/plan docs updated. No Rust/registry/sync-engine changes — a net deletion. (Supersedes row N's "free tier … iCloud only".) Migration: keep iCloud by pointing the macOS iCloud Drive folder at a folder under the projects root, the same pattern as OneDrive/Google Drive. |

| **Q — WebDAV cloud provider** | **complete** | 2026-06-20 | New paid (Pro+) cloud provider. **Rust** `integrations/webdav.rs`: dedicated SSRF-screened `reqwest` client (resolve-then-pin, numeric-literal screening, per-account private/LAN opt-in; loopback/link-local/cloud-metadata hard-blocked), Basic auth from keyring (service `webdav`), `quick-xml` multistatus parsing (XXE/entity-expansion-safe), MKCOL-chain on 409, per-segment percent-encoded hrefs, IPCs `webdav_{validate_host,status_probe,propfind,get,put,delete}` — 11 Rust unit tests. **Frontend** `cloud/webdav/`: `CloudFsProvider` with poll-and-diff delta (WebDAV file servers don't expose RFC 6578 sync-token — the `href→etag` snapshot is serialized into the engine cursor; ETag = `rev` for echo suppression), plus a connect-form flow. Wired into registry/init/create/settings (`CloudProviderId += webdav`; additive `cloud.accounts` fields `baseUrl`/`username`/`allowPrivateHost`) + a WebDAV row in IntegrationsPanel + the new-project cloud picker. Entitlement `integrations.cloud.webdav` seeded Pro+ in `../infrastructure/supabase/seed.sql`. SFTP was evaluated and **deferred** (it would reverse the standing no-SSH-transport decision and needs a new SSH stack + host-key trust). Verified: 53 Rust integration tests + 113 frontend tests + typecheck green; live end-to-end against a real server remains a user-driven check. |

| **R — Two-tier pricing (Team retired)** | **complete** | 2026-06-20 | Collapsed the plan catalog to **Free + Pro** only. Frontend: `Tier = "free" \| "pro"`; removed the Team branches in `PlanBadge` (AccountSection) + the `team` entry in `SubscriptionBadge`'s palette + the `templates.shared.publish`→`team` inference in `entitlements-source.ts`. Infra: `seed.sql` drops the Team plan row and every `('team', …)` entitlement; migration `20260620000002_drop_team_plan` migrates any team subscriptions → pro then deletes the Team plan (its `entitlements_map` rows cascade). Applied to staging — `plans` = `{free, pro}`, 0 team rows/subs; typecheck + 113 frontend tests green. The unshipped Team-only shared-templates surface is now locked for all (free=false/pro=false). |

| **S — Widgets panel unmounted** | **complete** | 2026-07-01 | The opt-in Widgets panel is removed from the Projects screen for now (user direction). `ProjectsScreen` no longer imports `DashboardPanel` and the toolbar "Widgets" toggle is gone, so `src/widgets/` drops out of the bundle entirely. The widget code (`DashboardPanel`, registry, builtins), the `workspace.dashboardEnabled`/`dashboardOrder`/`widgets`/`statsCards` settings plumbing, and users' persisted values are all left intact so the panel can be remounted later without migration. See design/widgets.md. |

| **T — Desktop-convention + a11y design pass** | **complete** | 2026-07-01 | Full 9-dimension design audit (color tokens, WCAG contrast math per theme, typography, spacing/layout/DPI, desktop-native feel, platform conventions, component consistency, feedback states, a11y) — 97 adversarially-verified findings fixed across 61 files. Highlights: **desktop feel** — webview context menu suppressed on chrome (kept on editable surfaces), `user-select:none` shell with content opt-ins (`select-text` on logs/errors/chat), `overscroll-behavior` none/contain, every `cursor-pointer` removed from controls, window `minWidth` 960→1024 so min-size desktop never flips into the tablet layout. **Contrast** — Aurora fg-3/fg-4/syntax-comment retuned for glass/popover composites; Daylight/Lamplight `--syntax-bracket`, Daylight `--accent-text-2`/`--format-latex`, Paper cyan deepened; **deepened light-theme accent-palette variants** (stock brights failed AA as text and made the focus ring ~1.6:1 on light themes); focus ring alpha 70→90% + covers `[role=button]`. **Type scale** — Tailwind `text-xs…xl` now alias the density tokens; whole live UI migrated off hardcoded px (13px absorbed into base; 10px stays as the micro floor). **Platform** — macOS modifier glyph order per HIG (⌥⇧⌘); Enter confirms dialog forms; Esc/focus-restore on all hand-rolled popovers + palette; editor tab strip and FileTree keyboard nav; dirty-buffer window-close guard (`onCloseRequested` + `core:window:allow-destroy`). **Components** — Dialog/LogsSheet scrims on `--color-overlay-scrim`; Button primary shadow tokenized (`--shadow-accent-btn`); AiView native `<select>` → house listbox; zoom/Export/Layout/Switcher menus got the listbox contract; shared `SoonBadge`; settings-row control height standardized h-8; danger actions on `variant="danger"`/`--color-danger-fill`. **Feedback** — toasts for project/file-open failures + watcher degradation; sign-out/plan-query error surfacing; TemplateGallery + ReferencesPanel loading/error states; new-file overwrite guard; `keepMine` conflict deletion confirms; compile pill + sync badge are `role="status"` live regions; AI chat aria-live. **Cleanup** — dead `.thumb`/`.doc-line`/`.shimmer`/`.composer-glow` classes, orphaned keyframes, unused radius tokens deleted; italic variable fonts imported; boot splash synced to post-AA dims. Deferred (features, not fixes): project delete/rename, thread-level review-gutter selection, NotificationsPanel focus management. |

| **V — Architecture audit + remediation** | **complete** | 2026-07-02 | Multi-agent 11-dimension architecture review (layering/seams, coupling, reactivity, IPC contract, Rust structure, concurrency/lifecycle, extensibility, errors/telemetry, testing, bundle/deps, security structure) — 70 adversarially-verified findings, then a full fix pass. **Reviews feature** (3 highs): single-owner thread anchors (CM RangeSet derived from the store, not a second source of truth); `requestReviewPanel` intent signal replaces the window CustomEvent; persistence no longer silent-wipes (write failure toasts+retries, load failure stays read-only); `persistenceBridge.destroy()` and window-close both flush pending saves. **Sync/buffer seam** (2 highs): save-after-pull base-hash conflict guard (`.conflict-<ISO>` sidecar), `SyncEngine.start()` reconciliation for dropped pushes, async `stop()` quiescence, project-scoped compile, conflict resolution moved into the engine. **Git/`.typeward`** (1 high): `.git/info/exclude` written on init/clone/open/import + `.typeward` filtered from status/clean-check/stage-all. **Errors**: one `describeIpcError` (`lib/errors.ts`), Rust commands reject with Display strings, `ErrorBoundary` at the shell, entitlement-refresh no longer silently downgrades paid users, aggregator failures surfaced. **Structure**: Kobalte-free toast service (`lib/toast.ts`), `activeFile` memo in a `createRoot` (kills the boot warning), idempotent autosave, shortcut-collision warnings, LSP zombie-session eviction, `commands.rs`→`compile.rs` split, framing dedup, single `outbound_client_builder`/`require_registered_root`/`detect::resolve_program` chokepoints, branded `NormalizedRelPath`, single-instance guard, config/dep hygiene + bundle-shape CI guard. Deferred (documented in CLAUDE.md): full plugin-fs re-route (30), settings-tree relocation (51), portable review storage (35). Verified: typecheck + build + full test suite green. |
| **U — Performance audit + remediation** | **complete** | 2026-07-02 | 7-dimension perf audit (startup, bundle, IPC, frontend runtime, Rust backend, memory/idle, perceived) — 20 adversarially-verified findings fixed. **Startup**: boot-critical JS cut 1,213→155 KB raw (390→58 KB gz) — CodeMirror + PDF.js vendor chunks off the entry graph (dynamic import in `boot.ts`'s review command; a first-priority `runtime-helpers` advancedChunks group pins Rolldown's NUL-prefixed virtual helper modules out of vendor chunks), `boot-theme` now a classic script at `public/boot-theme.js` (was silently bundled into the entry — the dark-splash re-tint was inert in release builds), SaveTemplateDialog lazy, ProjectsScreen chunk preload + `list_projects` overlapped with the settings IPC. **Visible behavior**: PDF preview no longer blanks white on recompile (same-layout double-buffer swap in PdfViewer `load()`); FileTree keeps expanded folders + focus across saves/compiles (stable row identity); save-status dot pulses only while unsaved (matches t1 busy-pulses/ok-static); project open shows an "Opening…" card instead of flashing "No project open"; references search debounced 200ms + 200-row cap with footer. **Backend**: synctex + fs-IO sync commands moved off the main event-loop thread (async + spawn_blocking); shared reqwest client for AI streams; git pollers visibility-gated (30s slow poll + focus refresh; minimize-pause in CommitPanel); cloud-sync push retry exponential 15s→5 min cap. **Hot paths**: sidebar Words/Lines single-pass + 300ms debounce (was 3 full-doc scans per keystroke); LSP didChange serializes at send time only; KaTeX render memo in md preview; FileTree click on an open file switches tabs without re-reading disk. |

## Notes

- Settings persistence safe across upgrades: every new field uses
  `#[serde(default)]` on the Rust side. Users with older `settings.json`
  files load with the new defaults silently.
- Density default is **Cozy**; tablets get **Comfortable** auto-applied only
  on first run (handled in Phase E when the picker UI lands).
- Animations default **on**. OS-level `prefers-reduced-motion` still wins.
