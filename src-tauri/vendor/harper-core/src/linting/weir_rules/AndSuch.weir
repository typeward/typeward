expr main (and the such)
let message "Did you mean `and such`?"
let description "Corrects `and the such` to `and such`."
let kind "Usage"
let becomes "and such"

test "some of the rvm site documentation about the available useful rvm environment variables and the such" "some of the rvm site documentation about the available useful rvm environment variables and such"
test "I've been trying to update my library to using patterns and the such, and as a preliminary" "I've been trying to update my library to using patterns and such, and as a preliminary"
